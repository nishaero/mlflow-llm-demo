apiVersion: v1
kind: Service
metadata:
  name: {{ include "llm-api.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "llm-api.fullname" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
      name: http
